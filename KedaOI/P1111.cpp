#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false), cin.tie(), cout.tie();
    int n;
    cin >> n;
    string str;
    for(int i = 0; i < n; i++) {
        cin >> str;
        cout << str << '\n';
    }
    return 0;
}