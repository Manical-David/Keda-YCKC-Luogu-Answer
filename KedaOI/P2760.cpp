#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false), cin.tie(), cout.tie();
    int a, b, c, d;
    cin >> a >> b >> c >> d;
    cout << (a + b) * (c + d);
    return 0;
}