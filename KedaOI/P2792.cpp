#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false), cin.tie(), cout.tie();
    double a, b;
    cin >> a >> b;
    cout << fixed << setprecision(3) << a / b * 100 << "%\n";
    return 0;
}