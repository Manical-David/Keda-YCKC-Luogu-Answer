#include <bits/stdc++.h>
using namespace std;
int main() {
    // ios::sync_with_stdio(false), cin.tie(0);
    freopen("aisle.in", "r", stdin);
    freopen("aisle.out", "w", stdout);
    int n, l, w, x, y, a;
    cin >> n >> l >> w >> x >> y >> a;
    int nx = l / x, ny = l / y;
    int maxaisle = 
    return 0;
}