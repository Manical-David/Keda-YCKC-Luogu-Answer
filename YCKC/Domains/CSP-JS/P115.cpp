#include <bits/stdc++.h>
using namespace std;
int main() {
    // ios::sync_with_stdio(false), cin.tie(0);
    int n, c;
    // cin >> n >> c;
    scanf("%d%d", &n, &c);
    vector <int> a(n, 0);
    for(int i = 0; i < n; i++) {
        scanf("%d", a[i]);
    }
    
    return 0;
}