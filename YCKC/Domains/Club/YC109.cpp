#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false), cin.tie(0);
    int i = 0;
    do {
        i += 2;
        cout << i << '\n';
    } while(i + 2 <= 100);
    return 0;
}