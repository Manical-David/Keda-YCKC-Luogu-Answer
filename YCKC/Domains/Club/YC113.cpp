#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false), cin.tie(0);
    int a, b;
    cin >> a >> b;
    for(int i = a; i <= b; i++) {
        cout << i << '\n';
    }
    return 0;
}