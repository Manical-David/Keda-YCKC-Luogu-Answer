#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false), cin.tie(0);
    double a, b;
    cin >> a >> b;
    cout << fixed << setprecision(1) << 2.5 * a + 1.5 * b;
    return 0;
}