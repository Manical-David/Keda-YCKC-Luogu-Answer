#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false), cin.tie(0);
    int n;
    cin >> n;
    cout << (n < 60 ? "mei chui niu" : "niu niu chui shang tian");
    return 0;
}