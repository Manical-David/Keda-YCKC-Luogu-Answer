#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false), cin.tie(), cout.tie();
    int a, x, y;
    cin >> a >> x >> y;
    cout << a * x + a * 2 * y;    
    return 0;
}