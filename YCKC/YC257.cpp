#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false), cin.tie(), cout.tie();
    int a, b;
    cin >> a >> b;
    cout << fixed << setprecision(1) << (float)a * 2.5 + (float)b * 1.5;
    return 0;
}