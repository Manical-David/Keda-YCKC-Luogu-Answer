#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false), cin.tie(), cout.tie();
    double f;
    cin >> f;
    cout << fixed << setprecision(5) << 5 * (f - 32) / 9;
    return 0;
}