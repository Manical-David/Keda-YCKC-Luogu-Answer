#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false), cin.tie(), cout.tie();
    double r;
    cin >> r;
    cout << fixed << setprecision(4) << 2 * r << ' ' << 2 * r * 3.14159 << ' ' << r * r * 3.14159;
    return 0;
}