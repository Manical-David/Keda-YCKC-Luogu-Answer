#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false), cin.tie(), cout.tie();
    double r;
    cin >> r;
    cout << fixed << setprecision(2) << (3.14 * r * r * r) / 3 * 4;
    return 0;
}