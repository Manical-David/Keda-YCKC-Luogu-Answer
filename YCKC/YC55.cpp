#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false), cin.tie(), cout.tie();
    double a, b;
    cin >> a >> b;
    printf("%.3lf\n", a / b);
    printf("%d", (int)b * 2);
    return 0;
}