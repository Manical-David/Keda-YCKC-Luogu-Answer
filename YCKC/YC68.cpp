#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false), cin.tie(), cout.tie();
    int a;
    cin >> a;
    if(a % 2 == 1) cout << "odd";
    else cout << "even";
    return 0;
}