#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false), cin.tie(0);
    int a, b, c;
    cin >> a >> b >> c;
    int tot = a * 0.2 + b * 0.3 + c * 0.5;
    cout << tot;
    return 0;
}